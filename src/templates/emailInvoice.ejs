<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice from Star Smile Care</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
      crossorigin="anonymous"
    />
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #ffffff;
      color: #333;
    }
    .email-container {
      background-color: #ffffff;
      max-width: 650px;
      margin: 0 auto;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    /* Add a gradient background or image */
    .email-container {
      background: linear-gradient(135deg, #f7f9fd, #ffffff); /* Example gradient */
    }
    .email-header {
      text-align: center;
      border-bottom: 2px solid #f1f1f1;
      padding-bottom: 10px;
      margin-bottom: 20px;
    }
    .email-header .logo {
      font-size: 30px;
      font-weight: bold;
      color: #4CAF50;
    }
    .email-body {
      margin-bottom: 20px;
    }
    .email-body p {
      margin: 5px 0;
    }
    .invoice-info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 20px;
    }
    .invoice-info .left {
      width: 48%;
    }
    .invoice-info .right {
      text-align: right;
      width: 48%;
    }
    .invoice-details {
      width: 100%;
      margin-top: 20px;
      border-collapse: collapse;
      background-color: #ffffff; /* White background for the table */
    }
    .invoice-details th, .invoice-details td {
      padding: 10px;
      border: 1px solid #f1f1f1;
    }
    .invoice-details th {
      background-color: #f8f9fa;
      text-align: left;
    }
    .total-row {
      font-weight: bold;
      background-color: #f8f9fa;
    }
    .footer {
      text-align: center;
      margin-top: 30px;
      font-size: 14px;
      color: #777;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- Header Section -->
    <div class="email-header">
      <div class="logo">Star Smile Care</div>
      <p style="font-size: 16px;">Invoice for your recent treatment at Star Smile Care</p>
    </div>

    <!-- Invoice Information -->
    <div class="email-body">
      <p>Hello <%= receiverName %>,</p>
      <p>Thank you for choosing Star Smile Care! Please find your invoice details below:</p>

      <div class="invoice-info">
        <div class="left">
          <p><strong>Invoice Number:</strong> <%= invoiceNumber %></p>
          <p><strong>Invoice Date:</strong> <%= date %></p>
          <p><strong>Due Date:</strong> <%= dueDate %></p>
        </div>
        <div class="right">
          <p><strong>Billing Address:</strong></p>
          <p><%= receiverName %><br />
            <%= receiverAddress %></p>
        </div>
      </div>

      <!-- Treatments Table -->
      <table class="invoice-details">
        <thead>
          <tr>
            <th style="width: 70%;">Treatment</th>
            <th style="width: 30%;">Cost</th>
          </tr>
        </thead>
        <tbody>
          <% treatments.forEach(treatment => { %>
          <tr>
            <td><%= treatment.name %></td>
            <td><%= treatment.cost %></td>
          </tr>
          <% }) %>

          <!-- Total Calculation -->
          <tr class="total-row">
            <td>Sub Total</td>
            <td><%= subTotal %></td>
          </tr>
          <tr>
            <td>Discount (<%= discount %>%)</td>
            <td>- <%= totalDiscountAmount %></td>
          </tr>
          <tr>
            <td>Tax (<%= tax %>%)</td>
            <td><%= totaltaxAmount %></td>
          </tr>
          <tr class="total-row">
            <td><strong>Total Amount</strong></td>
            <td><strong><%= totalAmount %></strong></td>
          </tr>

          <% if (paidAmount) { %>
          <tr>
            <td><strong>Amount Paid</strong></td>
            <td>- <%= paidAmount %></td>
          </tr>
          <% } %>

          <tr class="total-row">
            <td><strong>Amount Due</strong></td>
            <td><%= amountDue %></td>
          </tr>
        </tbody>
      </table>
    </div>

    <!-- Footer Section -->
    <div class="footer">
      <p>If you have any questions or need further assistance, feel free to contact us at <a href="mailto:info@starsmilecare.com">info@starsmilecare.com</a>.</p>
      <p>Best regards,<br />The Star Smile Care Team</p>
      <p>Star Smile Care & Implant Center | 123 Smile Road, Dental City</p>
    </div>
  </div>
</body>
</html>
